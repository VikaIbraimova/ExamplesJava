FirstDB4:пример вставки(INSERT),удалени€(DELETE),редактировани€(UPDATE) строк в таблице Books базы данных postgres3;пример выборки из таблицы + махинаци€ с датой(ex4) - —абуров,урок 15 часть 1;

FirstDB2:пример выборки строк из таблицы - проходим по всем строкам - —абуров, урок 15 часть 2

WorkWithJSON-Temp1:пример содержит три пакета:
	ex1 - ¬ данном примере используетс€ библиотека gson-2.6.2.jar,
		котора€ преобразует данные класса в json-формат и наоборот
	ex2 -  ¬ данном примере показываетс€ как можно распарсить HTML:
 		1)–аспарсим заголовок статьи+ссылку на эту статью
	 	ƒл€ этого будем использовать библиотеку jsoup-1.9.2.jar
 		ѕарсить будем сайт 4pda.ru
	ex3 - ѕример парсит сайт магазина Ћента:
 		1)«аходит на страницу с акци€ми
 		2)¬ыводит список всех актуальных акций магазина
